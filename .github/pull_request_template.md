## What does this change?

<!-- If this adds a language, "Adds <Language>" is a complete answer. -->

## Why?

<!-- The diff already says what. Explain the reason, or the bug it fixes. -->

## Checklist

- [ ] Branched from `develop` and targeting `develop`
- [ ] `python -m pytest` passes
- [ ] `python -m ruff check .` passes

<!-- Adding a language? Only these apply: -->
- [ ] `nishachar languages` lists it
- [ ] `nishachar run -l <id> -c '<hello world>'` prints the expected output
- [ ] The `image` is an official or widely used container image
